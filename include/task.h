#ifndef TASK_H
#define TASK_H

#include <string>
#include <vector>
#include "order.h"

namespace ShelfSelection {

/**
 * @brief Represents a picking task that can contain multiple orders
 */
class Task {
public:
    // Constructors, Destructor, Main operations and Configuration
    
private:
    // Member variables
};

} // namespace ShelfSelection

#endif // TASK_H
