#ifndef PUBLISHER_H
#define PUBLISHER_H

#include <string>
#include <vector>
#include "order.h"

namespace ShelfSelection {

/**
 * @brief Publishes orders from backlog based on simulation time
 */
class Publisher {
public:
    // Constructors, Destructor, Main operations and Configuration
    
private:
    // Member variables
};

} // namespace ShelfSelection

#endif // PUBLISHER_H
